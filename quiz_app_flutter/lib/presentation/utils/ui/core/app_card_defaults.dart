import 'package:flutter/material.dart';

class AppCardDefaults {
  static final shape = RoundedRectangleBorder(
    borderRadius: BorderRadius.circular(8),
  );

  static final double elevation = 4;
}
